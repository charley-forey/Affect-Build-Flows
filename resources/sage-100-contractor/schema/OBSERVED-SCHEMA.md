# Sage 100 Contractor — observed schema

**Generated by `derive_schema.py`. Do not edit by hand.**

Derived from `foundation/01-ingestion/Sage/Build_Sage_Test.Dataflow/mashup.pq` — the Power Query that already reads Sage in production.

Source instance: `NC-AFFECT-1\SAGE100CON`, database `Affect Group`, schema `dbo`.

> **Observed, not complete.** A column missing here only means our queries never
> touched it. `INFORMATION_SCHEMA` on the live database is still the authority —
> see [../INTEGRATION-NOTES.md](../INTEGRATION-NOTES.md).
>
> Classification is deliberately conservative: a name is called a navigation
> property only when we have proof (it appears as `table(key)`, or is expanded as a
> table). Short names in the wider tables — `budget`, `jobcst`, `emptme`, `schedl`
> and similar on `actrec` — are listed as scalar columns but are very likely
> table-valued relationships too. `INFORMATION_SCHEMA.COLUMNS` settles it.

## Confirmed tables

| Table | What it holds | Scalar columns seen | Referenced by |
|---|---|---|---|
| `dbo.acpinv` | Accounts Payable invoices — header. Screen 4-2 Payable Invoices. | 43 | Expenditure_AllTIme_raw |
| `dbo.acppmt` | Accounts Payable payments (vendor payments). Screen 4-3. | 68 | Payments |
| `dbo.acrinv` | Accounts Receivable invoices — header. Screen 3-2 Receivable Invoices. | 29 | Revenue_AllTime_raw |
| `dbo.acrpmt` | Accounts Receivable payments / cash receipts. Screen 3-3-1. | 18 | Reciepts |
| `dbo.actpay` | Vendor master. Screen 4-4 Vendors. | 113 | Dim_Sage_Vendors |
| `dbo.actrec` | Job / project master. Screen 3-5 Jobs. | 131 | Dim__Sage_Projects |

## Columns by table

### `dbo.acpinv`

Accounts Payable invoices — header. Screen 4-2 Payable Invoices.

Observed columns:

```
_idnum        acpi_u        adjust        amtpad        apinte        ca_tax        cmpamt        ctcnum
dscavl        dscrpt        dsctkn        duedte        freigh        gstamt        hldamt        hldbll
hldrem        hotlst        hstamt        insusr        invamt        invbal        invdte        invnum
jobnum        ntetxt        payee2        phsnum        pstamt        qstamt        rcpamt        shpnum
slstax        subttl        taxcde        updusr        usedst        usetax        usettl        usrdf1
usrdf2        usrnme        vndnum
```

Names our model gives them:

| Sage column | Our label |
|---|---|
| `amtpad` | Amount Paid |
| `ctcnum` | Contract Number |
| `dscrpt` | Description |
| `duedte` | Due Date |
| `invbal` | Invoice Balance |
| `invdte` | Invoiced Date |
| `jobnum` | Job Number |
| `pchord` | PO Number |
| `vndnum` | Vendor_ID |

Navigation properties (foreign keys the SQL connector exposes — each names a related table):

| Related table | Via |
|---|---|
| `acppmt` | _idnum, recnum |
| `actpay` | table-valued |
| `actrec` | table-valued |
| `apivln` | _idnum, recnum |
| `jobphs` | table-valued |
| `pchord` | table-valued |
| `scdpay` | _idnum, recnum |
| `taxdst` | table-valued |

### `dbo.acppmt`

Accounts Payable payments (vendor payments). Screen 4-3.

Observed columns:

```
_idnum        _idref        achbch        acpi_u        actper        adjust        amount        amtpad
apinte        aplcrd        btcnum        ca_tax        chkdte        chknum        cmpamt        ctcnum
depdte        dscavl        dscdte        dscrpt        dsctkn        duedte        entdte        freigh
gstamt        hldamt        hldbll        hldrem        hotlst        hstamt        insdte        insusr
invamt        invbal        invdte        invnet        invnum        invttl        invtyp        jobnum
lgrrec        ntetxt        payee2        phsnum        postyr        pstamt        qstamt        rcpamt
recnum        refnum        retain        setpay        shpnum        slstax        status        subttl
taxcde        ttlpad        upddte        updusr        usedst        usetax        usettl        usrdf1
usrdf2        usrnme        vndnum        vodrec
```

Names our model gives them:

| Sage column | Our label |
|---|---|
| `Vendor_ID` | Vendor ID |
| `actper` | Billing Period |
| `amtpad` | Amount Paid |
| `chkdte` | Payment Date |
| `dscrpt.1` | Description |
| `duedte` | Due Date |
| `invbal` | Invoice Balance |
| `invdte` | Invoice Date |
| `jobnum` | Job Number |
| `vndnum` | Vendor_ID |

Navigation properties (foreign keys the SQL connector exposes — each names a related table):

| Related table | Via |
|---|---|
| `acpinv` | recnum |
| `acppmt` | _idnum, recnum |
| `actpay` | table-valued |
| `actrec` | table-valued |
| `apivln` | _idnum, recnum |
| `jobphs` | table-valued |
| `pchord` | table-valued |
| `scdpay` | _idnum, recnum |
| `taxdst` | table-valued |

### `dbo.acrinv`

Accounts Receivable invoices — header. Screen 3-2 Receivable Invoices.

Observed columns:

```
_idnum        amtpad        dscavl        dsctkn        gstamt        gstsbj        hldamt        hldbll
hldrem        hotlst        hstamt        hstsbj        imgfle        insusr        invamt        invbal
invdte        ntetxt        phsnum        postyr        pstamt        pstsbj        refnum        slstax
subttl        updusr        usrdf1        usrdf2        usrnme
```

Names our model gives them:

| Sage column | Our label |
|---|---|
| `amtpad` | Amount Paid |
| `invbal` | Invoice Balance |
| `invdte` | Invoice Date |

Navigation properties (foreign keys the SQL connector exposes — each names a related table):

| Related table | Via |
|---|---|
| `pchord` | table-valued |
| `taxdst` | table-valued |

### `dbo.acrpmt`

Accounts Receivable payments / cash receipts. Screen 3-3-1.

Observed columns:

```
_idnum        _idref        actper        amount        aplcrd        chkdte        chknum        dscrpt
dsctkn        insdte        insusr        invdte        jobnum        lgrrec        postyr        recnum
upddte        updusr
```

Names our model gives them:

| Sage column | Our label |
|---|---|
| `actper` | Billing Period |
| `amount` | Amount |
| `chkdte` | Payment Date |
| `invdte` | Invoice Date |
| `jobnum` | Job Number |

### `dbo.actpay`

Vendor master. Screen 4-4 Vendors.

Observed columns:

```
_idnum        acheml        actnum        actp_u        actrec(recnum) 2  acttyp        addrs1        addrs2
aiafrm        aialin        begbal        bnkact        ca_tax        cdedft        cllphn        cmprte
condsc        contct        contyp        coresp        cstcde        csttyp        ctynme        dirdep
dscdte        dscrte        duedte        dupchk        e_mail        eftact        eftcde        efteml
eftiid        eftpay        eftrtn        elc199        eml199        endbal        eqpcst        eqpmnt
faxnum        fdrpsb        fedidn        homphn        hotlst        imgfle        inactv        insdte
insusr        intrnl        jobcst        lgrdft        lgrtrn        licnum        lonfrm        lonlin
minsts        ntetxt        orddsc        ordtyp        ownnme        pagnum        paymth        pchtyp
pclsln(recnum) 2  phnnum        plnrcd        plnrcv        pomesg        prente        prt199        rccpay
rcctrn        recnum        reqinf        reqprp        reqtyp        resnum        rfpdsc        rfptyp
rtnmbr        sbcgln        sbctyp        scdlen(recnum) 2  schsub        sepchk        shtnme        srvsch
state_        steidn        stsdft        subcon        submtl        taxcde        tkflin        tolamt
tolexc        tolprc        toltyp        trnmtl        typdft        untbll        upddte        updusr
usrdf1        usrdf2        utwarn        utxrte        vndnme        vndnum        vndprc        wrndft
zipcde
```

Names our model gives them:

| Sage column | Our label |
|---|---|
| `recnum` | Vendor ID |
| `vndnme` | Vendor Name |

Navigation properties (foreign keys the SQL connector exposes — each names a related table):

| Related table | Via |
|---|---|
| `actrec` | recnum |
| `lgract` | table-valued |
| `pchord` | table-valued |
| `pclsln` | recnum |
| `privnd` | _idnum, recnum |
| `scdlen` | recnum |
| `taxdst` | table-valued, taxdst |
| `vndbal` | _idnum, recnum |
| `vndcnt` | _idnum, recnum |
| `vndcrt` | _idnum, recnum |
| `vndrmt` | _idnum, recnum |
| `vndtyp` | table-valued, vndtyp |
| `vndytd` | _idnum, recnum |

### `dbo.actrec`

Job / project master. Screen 3-5 Jobs.

Observed columns:

```
_idnum        achtct        actbid        actdte        actprc        actr_u        addrs1        addrs2
aiafrm        awddte        begbal        biddte        budget        catxex        clnnum        cmpdte
cntrct        connum        contct        coresp        county        crtfid        cstcmp        csttye
csttyl        csttym        csttyo        csttys        ctcdte        ctynme        dlypyr        dsccnt
dscdte        dtecmp        duedte        emptme        endbal        eqprvw        estdte        estemp
faxnum        fednum        fedsck        finchg        fldrpt        hrscmp        imgfle        inactv
insdte        insusr        invalc        jobcst        jobnme        lcltax        lender        lenfld
lenrls        loctax        lonfrm        lotclr        lotnum        lotprm        modnme        ncrtck
ntetxt        pchlst        pctcmp        phnnum        plnprc        plnrcd        plnrcv        prmadd
prmchg        prmcty        prmeml        prmnme        prmphn        prmste        prmzip        propsl
pstexm        pstwip        ptotkf        rcapjb        rcarjb        rccjob        rccpay        rccrec
reccln        recnum        remids        reqinf        reqprp        retain        rtejob        rtervw
schedl        schprd        shtnme        slsemp        slstax        sprvsr        sqarft        srvinv
state_        status        stenum        stmeml        strdte        sttdte        subcnt        subcon
submtl        timmat        tmcdln        trnmtl        typwrk        untbll        untcmp        untlin
untprp        upddte        updusr        usrcs6        usrcs7        usrcs8        usrcs9        usrdf1
usrdf2        usrlst        zipcde
```

Navigation properties (foreign keys the SQL connector exposes — each names a related table):

| Related table | Via |
|---|---|
| `acpinv` | table-valued |
| `acrinv` | table-valued |
| `actpay` | achtct, lender |
| `biditm` | _idnum, recnum |
| `dptmnt` | dptmnt, table-valued |
| `employ` | estemp, slsemp, sprvsr |
| `export` | _idnum, recnum, table-valued |
| `jobcnt` | _idnum, recnum |
| `jobpgp` | _idnum, recnum |
| `jobphs` | _idnum, recnum |
| `jobtyp` | jobtyp, table-valued |
| `lgract` | lgract, table-valued |
| `pchord` | recnum, table-valued |
| `prelen` | _idnum, recnum, table-valued |
| `scdlen` | _idnum, recnum |
| `taxdst` | table-valued |
| `vndcrt` | table-valued |

## Related tables discovered through foreign keys

Never queried directly, but named by navigation properties on the tables above.
This is the closest thing we have to a table catalogue for the rest of the database.

| Table | Reached from N confirmed tables |
|---|---|
| `acpinv` | 2 |
| `acppmt` | 2 |
| `acrinv` | 1 |
| `actpay` | 3 |
| `actrec` | 3 |
| `apivln` | 2 |
| `biditm` | 1 |
| `dptmnt` | 1 |
| `employ` | 1 |
| `export` | 1 |
| `jobcnt` | 1 |
| `jobpgp` | 1 |
| `jobphs` | 3 |
| `jobtyp` | 1 |
| `lgract` | 2 |
| `pchord` | 5 |
| `pclsln` | 1 |
| `prelen` | 1 |
| `privnd` | 1 |
| `scdlen` | 2 |
| `scdpay` | 2 |
| `taxdst` | 5 |
| `vndbal` | 1 |
| `vndcnt` | 1 |
| `vndcrt` | 2 |
| `vndrmt` | 1 |
| `vndtyp` | 1 |
| `vndytd` | 1 |
